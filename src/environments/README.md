# Local Firebase configuration

Copy `environment.example.ts` to `environment.local.ts`, then replace the
placeholder values with the Firebase Web configuration for your own project.

`environment.local.ts` is intentionally ignored by Git and must not be
committed.
